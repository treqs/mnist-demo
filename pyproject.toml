[project]
name = "mnist-demo"
version = "0.1.0"
description = "Minimal MNIST pipeline for roar provenance demos"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "scikit-learn",
    "pyarrow",
    "Pillow",
    "wandb",
]
